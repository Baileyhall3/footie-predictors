import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bh.footiepredictors',
  appName: 'Footie Predictors',
  webDir: 'dist'
};

export default config;
